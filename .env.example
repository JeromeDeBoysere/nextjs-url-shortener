# Database Connection
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# You can use a local PostgreSQL instance or a cloud provider (Supabase, Vercel Postgres, Railway, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/url_shortener"

# Application Configuration
# Base URL for the application (used for generating short links)
# In production, this should be your domain (e.g., https://yourdomain.com/)
# In development, use http://localhost:3000/
NEXT_PUBLIC_BASE_URL="http://localhost:3000/"

# ============================================================
# SUPABASE CONFIGURATION
# ============================================================
# These variables must be set in Vercel Dashboard:
# Vercel > Project > Settings > Environment Variables
#
# NEXT_PUBLIC_SUPABASE_URL        → Supabase.com > Project Settings > Data API > Project URL (e.g. https://abc.supabase.co)
# NEXT_PUBLIC_SUPABASE_ANON_KEY   → Supabase.com > Project Settings > API Keys > Publishable key (e.g. sb_publishable_xyz)
# ============================================================
